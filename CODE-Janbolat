<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSS Фильтры</title>
    <style>
        body {
            text-align: center;
            margin-top: 50px;
        }

        .container {
            position: relative;
            display: inline-block;
        }

        img {
            width: 500px;
            height: auto;
            animation: filterChange 10s infinite steps(10);
        }

        @keyframes filterChange {
            0% { filter: blur(2px); }
            10% { filter: brightness(150%); }
            20% { filter: drop-shadow(8px 8px 10px gray); }
            30% { filter: grayscale(1); }
            40% { filter: hue-rotate(200deg); }
            50% { filter: invert(70%); }
            60% { filter: opacity(50%); }
            70% { filter: saturate(100%); }
            80% { filter: sepia(1); }
            90% { filter: contrast(150%); }
            100% { filter: blur(2px); }
        }

        button {
            margin-top: 20px;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1>filter</h1>
    <div class="container">
        <img id="image" src="img-1.jpeg">
    </div>
    <br>
    <button>Сүзгіні өзгерту</button>
</body>
</html>
